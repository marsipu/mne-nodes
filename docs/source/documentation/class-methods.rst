Class-Methods
=============

For selected classes from mne, it is possible to use their methods as function-nodes. They accept the lowercase class-name as input (e.g. Raw → raw) and give the changed instance as output. To further adapt these methods, you can of course always write a custom function.
